import HeroTitle from "./HeroTitle";

export default function Hero() {
  return (
    <section className="flex justify-center items-center w-full min-h-[156px] bg-primary">
      <HeroTitle />
    </section>
  );
}
